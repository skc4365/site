# requirements 패키지별 용도

각 줄의 버전 조건 뒤에 해당 패키지의 주요 기능과 사용 목적을 주석으로 표시했습니다.

```text
absl-py==2.5.0  # absl-py을(를) 사용하는 상위 패키지의 보조 의존성
accelerate==1.7.0  # PyTorch 모델의 GPU·분산 학습 및 추론 간소화
aiofiles==25.1.0  # 비동기 파일 입출력
aiohappyeyeballs==2.7.1  # 비동기 네트워크·이벤트 처리 지원
aiohttp==3.14.3  # 비동기 HTTP 클라이언트·서버
aiosignal==1.4.0  # 비동기 네트워크·이벤트 처리 지원
altair==5.5.0  # 선언형 데이터 시각화
annotated-types==0.7.0  # 타입 검사·데이터 모델·직렬화 지원
anthropic==0.125.0  # Anthropic Claude API SDK
antlr4-python3-runtime==4.9.3  # antlr4-python3-runtime을(를) 사용하는 상위 패키지의 보조 의존성
anyio==4.9.0  # 비동기 네트워크·이벤트 처리 지원
appdirs==1.4.4  # 파일·패키지·빌드 환경 관리 지원
argon2-cffi==23.1.0  # 인증·암호화·토큰과 자격 증명 처리
argon2-cffi-bindings==21.2.0  # 인증·암호화·토큰과 자격 증명 처리
arrow==1.3.0  # arrow을(를) 사용하는 상위 패키지의 보조 의존성
arxiv==2.1.3  # arXiv 논문 검색·조회
asgiref==3.8.1  # asgiref을(를) 사용하는 상위 패키지의 보조 의존성
asttokens==3.0.0  # 개발·디버깅·소스 분석 지원
async-lru==2.0.5  # 비동기 네트워크·이벤트 처리 지원
attrs==26.1.0  # 타입 검사·데이터 모델·직렬화 지원
azure-ai-inference==1.0.0b9  # Azure의 ai-inference 서비스·공통 기능 연동
azure-core==1.34.0  # Azure의 core 서비스·공통 기능 연동
azure-core-tracing-opentelemetry==1.0.0b12  # Azure의 core-tracing-opentelemetry 서비스·공통 기능 연동
azure-cosmos==4.16.3  # Azure의 cosmos 서비스·공통 기능 연동
azure-identity==1.25.3  # Azure의 identity 서비스·공통 기능 연동
babel==2.17.0  # babel을(를) 사용하는 상위 패키지의 보조 의존성
backoff==2.2.1  # backoff을(를) 사용하는 상위 패키지의 보조 의존성
bcrypt==4.3.0  # 인증·암호화·토큰과 자격 증명 처리
beautifulsoup4==4.15.0  # HTML·XML 파싱과 웹 데이터 추출
bleach==6.2.0  # bleach을(를) 사용하는 상위 패키지의 보조 의존성
blinker==1.9.0  # blinker을(를) 사용하는 상위 패키지의 보조 의존성
build==1.2.2.post1  # 파일·패키지·빌드 환경 관리 지원
cachetools==5.5.2  # cachetools을(를) 사용하는 상위 패키지의 보조 의존성
certifi==2025.4.26  # HTTP·웹 통신과 인증서·인코딩 처리 지원
cffi==2.1.1  # cffi을(를) 사용하는 상위 패키지의 보조 의존성
chardet==7.6.0  # HTTP·웹 통신과 인증서·인코딩 처리 지원
charset-normalizer==3.4.2  # HTTP·웹 통신과 인증서·인코딩 처리 지원
chroma-hnswlib==0.7.6  # chroma-hnswlib을(를) 사용하는 상위 패키지의 보조 의존성
chromadb==0.6.3  # 임베딩 저장·검색용 벡터 데이터베이스
click==8.4.2  # click을(를) 사용하는 상위 패키지의 보조 의존성
cohere==5.15.0  # Cohere 생성·임베딩·재순위화 API
colorama==0.4.6  # colorama을(를) 사용하는 상위 패키지의 보조 의존성
coloredlogs==15.0.1  # coloredlogs을(를) 사용하는 상위 패키지의 보조 의존성
comm==0.2.2  # Jupyter 노트북 실행·통신·문서 처리 지원
contourpy==1.3.3  # 데이터 시각화와 그래프 렌더링 지원
cryptography==50.0.0  # 인증·암호화·토큰과 자격 증명 처리
cycler==0.12.1  # 데이터 시각화와 그래프 렌더링 지원
dataclasses-json==0.6.7  # 타입 검사·데이터 모델·직렬화 지원
datasets==5.0.1  # Hugging Face 데이터셋 처리
debugpy==1.8.14  # 개발·디버깅·소스 분석 지원
decorator==5.2.1  # decorator을(를) 사용하는 상위 패키지의 보조 의존성
deepl==1.21.1  # DeepL 번역 API
defusedxml==0.7.1  # defusedxml을(를) 사용하는 상위 패키지의 보조 의존성
Deprecated==1.2.18  # Deprecated을(를) 사용하는 상위 패키지의 보조 의존성
dill==0.3.8  # dill을(를) 사용하는 상위 패키지의 보조 의존성
dirtyjson==1.0.8  # dirtyjson을(를) 사용하는 상위 패키지의 보조 의존성
kiwipiepy-model==0.20.0  # kiwipiepy-model을(를) 사용하는 상위 패키지의 보조 의존성
distro==1.9.0  # distro을(를) 사용하는 상위 패키지의 보조 의존성
docstring-parser==0.18.0  # docstring-parser을(를) 사용하는 상위 패키지의 보조 의존성
dnspython==2.7.0  # dnspython을(를) 사용하는 상위 패키지의 보조 의존성
docx2txt==0.8  # DOCX 텍스트·이미지 추출
durationpy==0.10  # durationpy을(를) 사용하는 상위 패키지의 보조 의존성
elastic-transport==8.17.1  # elastic-transport을(를) 사용하는 상위 패키지의 보조 의존성
elasticsearch==8.19.3  # Elasticsearch 검색·색인 클라이언트
emoji==2.15.0  # emoji을(를) 사용하는 상위 패키지의 보조 의존성
et_xmlfile==2.0.0  # et_xmlfile을(를) 사용하는 상위 패키지의 보조 의존성
executing==2.2.0  # 개발·디버깅·소스 분석 지원
faiss-cpu==1.10.0  # CPU 기반 고속 벡터 유사도 검색
fastapi==0.115.12  # 비동기 REST API 프레임워크
fastavro==1.11.1  # 구조화 데이터 검증·직렬화·설정 처리
fastjsonschema==2.21.1  # fastjsonschema을(를) 사용하는 상위 패키지의 보조 의존성
feedparser==6.0.14  # 문서·HTML·XML 형식 파싱 및 변환
feedparser-sgmllib==2.1.0  # 문서·HTML·XML 형식 파싱 및 변환
filelock==3.32.4  # 파일·패키지·빌드 환경 관리 지원
filetype==1.2.0  # 파일·패키지·빌드 환경 관리 지원
FlashRank==0.2.10  # 경량 모델 기반 검색 결과 재순위화
flatbuffers==25.12.19  # 구조화 데이터 검증·직렬화·설정 처리
fonttools==4.63.0  # 데이터 시각화와 그래프 렌더링 지원
fqdn==1.5.1  # fqdn을(를) 사용하는 상위 패키지의 보조 의존성
frozenlist==1.8.0  # 비동기 네트워크·이벤트 처리 지원
fsspec==2026.7.0  # 로컬·클라우드 저장소 공통 파일 API
ftfy==6.3.1  # ftfy을(를) 사용하는 상위 패키지의 보조 의존성
gitdb==4.0.12  # gitdb을(를) 사용하는 상위 패키지의 보조 의존성
GitPython==3.1.59  # Python에서 Git 저장소 조작
google-ai-generativelanguage==0.6.18  # Google API 인증·통신 또는 생성형 AI 연동
google-api-core==1.34.1  # Google API 인증·통신 또는 생성형 AI 연동
google-auth==2.40.1  # Google API 인증·통신 또는 생성형 AI 연동
google_search_results==2.4.2  # SerpApi 기반 Google 검색
googleapis-common-protos==1.70.0  # Google API 인증·통신 또는 생성형 AI 연동
grandalf==0.8  # grandalf을(를) 사용하는 상위 패키지의 보조 의존성
greenlet==3.2.2  # greenlet을(를) 사용하는 상위 패키지의 보조 의존성
grpc-gateway-protoc-gen-openapiv2==0.1.0  # grpc-gateway-protoc-gen-openapiv2을(를) 사용하는 상위 패키지의 보조 의존성
grpcio==1.67.1  # grpcio을(를) 사용하는 상위 패키지의 보조 의존성
grpcio-status==1.48.2  # grpcio-status을(를) 사용하는 상위 패키지의 보조 의존성
h11==0.16.0  # h11을(를) 사용하는 상위 패키지의 보조 의존성
html5lib==1.1  # 문서·HTML·XML 형식 파싱 및 변환
httpcore==1.0.9  # HTTP·웹 통신과 인증서·인코딩 처리 지원
httpcore2==2.12.0  # HTTP·웹 통신과 인증서·인코딩 처리 지원
httptools==0.6.4  # HTTP·웹 통신과 인증서·인코딩 처리 지원
httpx==0.28.1  # 동기·비동기 HTTP 클라이언트
httpx2==2.12.0  # HTTP·웹 통신과 인증서·인코딩 처리 지원
httpx-sse==0.4.3  # HTTP·웹 통신과 인증서·인코딩 처리 지원
huggingface-hub==0.31.4  # Hugging Face 모델·데이터셋 관리
humanfriendly==10.0  # humanfriendly을(를) 사용하는 상위 패키지의 보조 의존성
idna==3.10  # HTTP·웹 통신과 인증서·인코딩 처리 지원
importlib_metadata==8.6.1  # 파일·패키지·빌드 환경 관리 지원
importlib_resources==6.5.2  # 파일·패키지·빌드 환경 관리 지원
ipykernel==6.29.5  # Jupyter 노트북 실행·통신·문서 처리 지원
ipython==9.2.0  # Jupyter 노트북 실행·통신·문서 처리 지원
ipython_pygments_lexers==1.1.1  # ipython_pygments_lexers을(를) 사용하는 상위 패키지의 보조 의존성
ipywidgets==8.1.9  # Jupyter 대화형 UI 위젯
isodate==0.7.2  # isodate을(를) 사용하는 상위 패키지의 보조 의존성
isoduration==20.11.0  # isoduration을(를) 사용하는 상위 패키지의 보조 의존성
jedi==0.19.2  # 개발·디버깅·소스 분석 지원
Jinja2==3.1.6  # Jinja2을(를) 사용하는 상위 패키지의 보조 의존성
jiter==0.16.0  # jiter을(를) 사용하는 상위 패키지의 보조 의존성
joblib==1.5.3  # 머신러닝 모델·수치 연산·추론 지원
jpype1==1.7.1  # jpype1을(를) 사용하는 상위 패키지의 보조 의존성
json5==0.12.0  # 구조화 데이터 검증·직렬화·설정 처리
jsonpatch==1.33  # 구조화 데이터 검증·직렬화·설정 처리
jsonpointer==3.0.0  # 구조화 데이터 검증·직렬화·설정 처리
jsonschema==4.23.0  # 구조화 데이터 검증·직렬화·설정 처리
jsonschema-specifications==2025.4.1  # 구조화 데이터 검증·직렬화·설정 처리
jupyter==1.1.1  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyter-console==6.6.3  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyter-events==0.12.0  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyter-lsp==2.2.5  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyter_client==8.6.3  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyter_core==5.7.2  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyter_server==2.16.0  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyter_server_terminals==0.5.3  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyterlab==4.3.6  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyterlab_pygments==0.3.0  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyterlab_server==2.27.3  # Jupyter 노트북 실행·통신·문서 처리 지원
jupyterlab_widgets==3.0.17  # Jupyter 노트북 실행·통신·문서 처리 지원
kiwipiepy==0.20.4  # 한국어 형태소 분석·토큰화
kiwisolver==1.5.0  # 데이터 시각화와 그래프 렌더링 지원
konlpy==0.6.0  # 한국어 자연어 처리 도구
kubernetes==32.0.1  # Kubernetes API 클라이언트
langchain==1.3.15  # LLM 체인·검색·에이전트 개발 프레임워크
langchain-anthropic==0.3.10  # LangChain의 anthropic 서비스·기능 연동
langchain-azure-ai==0.1.2  # LangChain의 azure-ai 서비스·기능 연동
langchain-chroma==0.2.2  # LangChain의 chroma 서비스·기능 연동
langchain-cohere==0.4.4  # LangChain의 cohere 서비스·기능 연동
langchain-community==0.4.2  # LangChain의 community 서비스·기능 연동
langchain-core==1.6.0  # LangChain의 core 서비스·기능 연동
langchain-elasticsearch==0.3.2  # LangChain의 elasticsearch 서비스·기능 연동
langchain-experimental==0.4.2  # LangChain의 experimental 서비스·기능 연동
langchain-google-genai==2.1.0  # LangChain의 google-genai 서비스·기능 연동
langchain-huggingface==0.1.2  # LangChain의 huggingface 서비스·기능 연동
langchain-milvus==0.1.8  # LangChain의 milvus 서비스·기능 연동
langchain-ollama==0.2.3  # LangChain의 ollama 서비스·기능 연동
langchain-openai==1.6.0  # LangChain의 openai 서비스·기능 연동
langchain-teddynote==0.3.44  # LangChain의 teddynote 서비스·기능 연동
langchain-text-splitters==1.1.2  # LangChain의 text-splitters 서비스·기능 연동
langchainhub==0.1.21  # LangChain Hub 프롬프트 조회·공유
langdetect==1.0.9  # langdetect을(를) 사용하는 상위 패키지의 보조 의존성
langgraph==1.2.11  # 상태 그래프 기반 AI 에이전트 워크플로
langgraph-checkpoint==2.0.26  # LangGraph의 checkpoint 기능 지원
langgraph-prebuilt==0.1.8  # LangGraph의 prebuilt 기능 지원
langgraph-sdk==0.1.69  # LangGraph의 sdk 기능 지원
langsmith==0.3.42  # LLM 실행 추적·평가·데이터셋 관리
lark==1.2.2  # lark을(를) 사용하는 상위 패키지의 보조 의존성
llama-cloud==0.1.19  # llama-cloud을(를) 사용하는 상위 패키지의 보조 의존성
llama-cloud-services==0.6.21  # llama-cloud-services을(를) 사용하는 상위 패키지의 보조 의존성
llama-index-core==0.11.23  # LlamaIndex 인덱싱·검색 핵심 기능
llama-index-readers-file==0.2.2  # llama-index-readers-file을(를) 사용하는 상위 패키지의 보조 의존성
llama-parse==0.6.4.post1  # LlamaParse 문서 파싱 서비스
lxml==6.1.2  # 문서·HTML·XML 형식 파싱 및 변환
lz4==4.4.4  # lz4을(를) 사용하는 상위 패키지의 보조 의존성
markdown-it-py==3.0.0  # markdown-it-py을(를) 사용하는 상위 패키지의 보조 의존성
MarkupSafe==3.0.3  # MarkupSafe을(를) 사용하는 상위 패키지의 보조 의존성
marshmallow==3.26.2  # 타입 검사·데이터 모델·직렬화 지원
matplotlib==3.10.3  # 그래프와 차트 생성
matplotlib-inline==0.1.7  # 데이터 시각화와 그래프 렌더링 지원
mdurl==0.1.2  # mdurl을(를) 사용하는 상위 패키지의 보조 의존성
mistune==3.1.3  # mistune을(를) 사용하는 상위 패키지의 보조 의존성
ml_dtypes==0.5.1  # 머신러닝 모델·수치 연산·추론 지원
mmh3==4.1.0  # mmh3을(를) 사용하는 상위 패키지의 보조 의존성
mpmath==1.3.0  # mpmath을(를) 사용하는 상위 패키지의 보조 의존성
msal==1.35.1  # 인증·암호화·토큰과 자격 증명 처리
msal-extensions==1.3.1  # 인증·암호화·토큰과 자격 증명 처리
multidict==6.7.1  # 비동기 네트워크·이벤트 처리 지원
multiprocess==0.70.16  # multiprocess을(를) 사용하는 상위 패키지의 보조 의존성
mypy==1.15.0  # 타입 검사·데이터 모델·직렬화 지원
mypy_extensions==1.1.0  # 타입 검사·데이터 모델·직렬화 지원
narwhals==1.40.0  # narwhals을(를) 사용하는 상위 패키지의 보조 의존성
nbclient==0.10.2  # Jupyter 노트북 실행·통신·문서 처리 지원
nbconvert==7.16.6  # Jupyter 노트북 실행·통신·문서 처리 지원
nbformat==5.10.4  # Jupyter 노트북 실행·통신·문서 처리 지원
nest-asyncio==1.6.0  # nest-asyncio을(를) 사용하는 상위 패키지의 보조 의존성
networkx==3.6.1  # 그래프 자료구조와 알고리즘
nltk==3.10.3  # 자연어 처리·말뭉치·토큰화
notebook==7.3.3  # notebook을(를) 사용하는 상위 패키지의 보조 의존성
notebook_shim==0.2.4  # notebook_shim을(를) 사용하는 상위 패키지의 보조 의존성
numpy==1.26.4  # 배열 기반 고성능 수치 계산
oauthlib==3.2.2  # 인증·암호화·토큰과 자격 증명 처리
olefile==0.47  # 문서·HTML·XML 형식 파싱 및 변환
ollama==0.6.2  # 로컬 Ollama 모델 서버 클라이언트
omegaconf==2.3.0  # omegaconf을(를) 사용하는 상위 패키지의 보조 의존성
onnx==1.17.0  # 머신러닝 모델·수치 연산·추론 지원
onnxruntime==1.26.0  # ONNX 모델 추론 엔진
open_clip_torch==2.31.0  # CLIP 이미지·텍스트 임베딩
openai==3.3.1  # OpenAI API SDK
opencv-python==4.11.0.86  # 이미지·영상 처리와 컴퓨터 비전
opentelemetry-api==1.33.1  # OpenTelemetry 기반 요청 추적·관측성 지원
opentelemetry-exporter-otlp-proto-grpc==1.15.0  # OpenTelemetry 기반 요청 추적·관측성 지원
opentelemetry-instrumentation==0.54b1  # OpenTelemetry 기반 요청 추적·관측성 지원
opentelemetry-instrumentation-asgi==0.54b1  # OpenTelemetry 기반 요청 추적·관측성 지원
opentelemetry-instrumentation-fastapi==0.54b1  # OpenTelemetry 기반 요청 추적·관측성 지원
opentelemetry-proto==1.15.0  # OpenTelemetry 기반 요청 추적·관측성 지원
opentelemetry-sdk==1.33.1  # OpenTelemetry 기반 요청 추적·관측성 지원
opentelemetry-semantic-conventions==0.54b1  # OpenTelemetry 기반 요청 추적·관측성 지원
opentelemetry-util-http==0.54b1  # OpenTelemetry 기반 요청 추적·관측성 지원
orjson==3.10.18  # 구조화 데이터 검증·직렬화·설정 처리
ormsgpack==1.9.1  # 구조화 데이터 검증·직렬화·설정 처리
overrides==7.7.0  # overrides을(를) 사용하는 상위 패키지의 보조 의존성
packaging==25.0  # 파일·패키지·빌드 환경 관리 지원
pandas==2.2.3  # 표 형태 데이터 분석·가공
pandocfilters==1.5.1  # pandocfilters을(를) 사용하는 상위 패키지의 보조 의존성
parso==0.8.4  # 개발·디버깅·소스 분석 지원
pdf2image==1.17.0  # PDF 페이지를 이미지로 변환
pdfminer.six==20231228  # 문서·HTML·XML 형식 파싱 및 변환
pdfplumber==0.11.5  # PDF 텍스트·표·좌표 추출
pi_heif==1.4.0  # pi_heif을(를) 사용하는 상위 패키지의 보조 의존성
pillow==12.3.0  # 이미지 열기·변환·크기 조정
pinecone==5.4.2  # Pinecone 벡터 DB SDK
pinecone-client==3.2.2  # Pinecone 벡터 검색·추론 기능 지원
pinecone-plugin-inference==3.1.0  # Pinecone 벡터 검색·추론 기능 지원
pinecone-plugin-interface==0.0.7  # Pinecone 벡터 검색·추론 기능 지원
pinecone-text==0.11.0  # Pinecone 벡터 검색·추론 기능 지원
platformdirs==4.3.8  # 파일·패키지·빌드 환경 관리 지원
posthog==4.0.1  # posthog을(를) 사용하는 상위 패키지의 보조 의존성
prometheus_client==0.22.0  # prometheus_client을(를) 사용하는 상위 패키지의 보조 의존성
prompt_toolkit==3.0.51  # prompt_toolkit을(를) 사용하는 상위 패키지의 보조 의존성
propcache==0.5.2  # propcache을(를) 사용하는 상위 패키지의 보조 의존성
proto-plus==1.26.1  # 구조화 데이터 검증·직렬화·설정 처리
protobuf==7.36.0  # 구조화 데이터 검증·직렬화·설정 처리
psutil==7.0.0  # psutil을(를) 사용하는 상위 패키지의 보조 의존성
pure_eval==0.2.3  # 개발·디버깅·소스 분석 지원
pyarrow==21.0.0  # pyarrow을(를) 사용하는 상위 패키지의 보조 의존성
pyasn1==0.6.1  # pyasn1을(를) 사용하는 상위 패키지의 보조 의존성
pyasn1_modules==0.4.2  # pyasn1_modules을(를) 사용하는 상위 패키지의 보조 의존성
pyclipper==1.4.0  # pyclipper을(를) 사용하는 상위 패키지의 보조 의존성
pycocotools==2.0.8  # pycocotools을(를) 사용하는 상위 패키지의 보조 의존성
pycparser==3.0  # pycparser을(를) 사용하는 상위 패키지의 보조 의존성
pydantic==2.11.4  # 타입 힌트 기반 데이터 검증
pydantic-settings==2.15.0  # 환경 변수 기반 설정 관리
pydantic_core==2.33.2  # 타입 검사·데이터 모델·직렬화 지원
pydeck==0.9.3  # 데이터 시각화와 그래프 렌더링 지원
Pygments==2.19.1  # 개발·디버깅·소스 분석 지원
PyJWT==2.9.0  # 인증·암호화·토큰과 자격 증명 처리
pymilvus==2.5.5  # Milvus 벡터 DB SDK
pymongo==4.17.0  # MongoDB 공식 드라이버
PyMuPDF==1.25.4  # PDF 렌더링·텍스트 및 이미지 추출
pymupdf4llm==0.0.17  # PDF를 LLM용 Markdown으로 변환
pyparsing==3.3.2  # pyparsing을(를) 사용하는 상위 패키지의 보조 의존성
pypdf==6.16.2  # PDF 병합·분할·텍스트 처리
pypdfium2==5.13.0  # 문서·HTML·XML 형식 파싱 및 변환
PyPika==0.48.9  # PyPika을(를) 사용하는 상위 패키지의 보조 의존성
pyproject_hooks==1.2.0  # 파일·패키지·빌드 환경 관리 지원
pyreadline3==3.5.4  # pyreadline3을(를) 사용하는 상위 패키지의 보조 의존성
python-dateutil==2.9.0.post0  # python-dateutil을(를) 사용하는 상위 패키지의 보조 의존성
python-dotenv==1.2.3  # .env 환경 변수 로드
python-iso639==2026.7.23  # python-iso639을(를) 사용하는 상위 패키지의 보조 의존성
python-json-logger==3.3.0  # python-json-logger을(를) 사용하는 상위 패키지의 보조 의존성
python-magic==0.4.27  # python-magic을(를) 사용하는 상위 패키지의 보조 의존성
python-multipart==0.0.20  # python-multipart을(를) 사용하는 상위 패키지의 보조 의존성
python-oxmsg==0.0.2  # python-oxmsg을(를) 사용하는 상위 패키지의 보조 의존성
pytz==2026.3.post1  # pytz을(를) 사용하는 상위 패키지의 보조 의존성
pywin32==310  # pywin32을(를) 사용하는 상위 패키지의 보조 의존성
pywinpty==2.0.15  # pywinpty을(를) 사용하는 상위 패키지의 보조 의존성
PyYAML==6.0.2  # PyYAML을(를) 사용하는 상위 패키지의 보조 의존성
pyzmq==26.4.0  # pyzmq을(를) 사용하는 상위 패키지의 보조 의존성
ragas==0.2.14  # RAG 응답 품질 평가
rank-bm25==0.2.2  # BM25 키워드 문서 검색
RapidFuzz==3.14.5  # RapidFuzz을(를) 사용하는 상위 패키지의 보조 의존성
rapidocr-onnxruntime==1.4.4  # ONNX 기반 문서 OCR
redis==5.2.1  # Redis 데이터베이스·캐시 클라이언트
referencing==0.36.2  # referencing을(를) 사용하는 상위 패키지의 보조 의존성
regex==2026.7.19  # regex을(를) 사용하는 상위 패키지의 보조 의존성
requests==2.32.3  # 동기 HTTP 요청 클라이언트
requests-oauthlib==2.0.0  # HTTP·웹 통신과 인증서·인코딩 처리 지원
requests-toolbelt==1.0.0  # HTTP·웹 통신과 인증서·인코딩 처리 지원
rfc3339-validator==0.1.4  # rfc3339-validator을(를) 사용하는 상위 패키지의 보조 의존성
rfc3986-validator==0.1.1  # rfc3986-validator을(를) 사용하는 상위 패키지의 보조 의존성
rich==14.0.0  # 터미널 표·색상·진행률 출력
rouge_score==0.1.2  # rouge_score을(를) 사용하는 상위 패키지의 보조 의존성
rpds-py==0.25.1  # rpds-py을(를) 사용하는 상위 패키지의 보조 의존성
rsa==4.9.1  # 인증·암호화·토큰과 자격 증명 처리
safetensors==0.8.0  # 머신러닝 모델·수치 연산·추론 지원
scikit-learn==1.6.1  # 머신러닝 알고리즘과 평가
scipy==1.15.3  # 머신러닝 모델·수치 연산·추론 지원
seaborn==0.13.2  # 데이터 시각화와 그래프 렌더링 지원
Send2Trash==1.8.3  # Send2Trash을(를) 사용하는 상위 패키지의 보조 의존성
sentence-transformers==5.7.0  # 문장·문서 임베딩 모델
sgmllib3k==1.0.0  # sgmllib3k을(를) 사용하는 상위 패키지의 보조 의존성
shapely==2.1.2  # shapely을(를) 사용하는 상위 패키지의 보조 의존성
shellingham==1.5.4  # shellingham을(를) 사용하는 상위 패키지의 보조 의존성
simsimd==6.2.1  # simsimd을(를) 사용하는 상위 패키지의 보조 의존성
six==1.17.0  # six을(를) 사용하는 상위 패키지의 보조 의존성
smmap==5.0.2  # smmap을(를) 사용하는 상위 패키지의 보조 의존성
sniffio==1.3.1  # 비동기 네트워크·이벤트 처리 지원
soupsieve==2.9.2  # soupsieve을(를) 사용하는 상위 패키지의 보조 의존성
SQLAlchemy==2.0.41  # SQL ORM과 쿼리 도구
stack-data==0.6.3  # 개발·디버깅·소스 분석 지원
starlette==0.46.2  # starlette을(를) 사용하는 상위 패키지의 보조 의존성
streamlit==1.43.2  # Python 기반 데이터·AI 웹 UI
striprtf==0.0.26  # 문서·HTML·XML 형식 파싱 및 변환
sympy==1.14.0  # 머신러닝 모델·수치 연산·추론 지원
tavily-python==0.7.27  # AI 에이전트용 웹 검색 API
tenacity==8.5.0  # tenacity을(를) 사용하는 상위 패키지의 보조 의존성
terminado==0.18.1  # terminado을(를) 사용하는 상위 패키지의 보조 의존성
threadpoolctl==3.6.0  # 머신러닝 모델·수치 연산·추론 지원
tiktoken==0.14.0  # OpenAI 모델 토큰 계산
timm==1.0.15  # 머신러닝 모델·수치 연산·추론 지원
tinycss2==1.4.0  # tinycss2을(를) 사용하는 상위 패키지의 보조 의존성
tokenizers==0.21.4  # 머신러닝 모델·수치 연산·추론 지원
toml==0.10.2  # 구조화 데이터 검증·직렬화·설정 처리
torch==2.2.0  # 딥러닝 텐서 연산·모델 학습
torchvision==0.17.0  # PyTorch 이미지 처리·비전 모델
tornado==6.5  # tornado을(를) 사용하는 상위 패키지의 보조 의존성
tqdm==4.70.0  # tqdm을(를) 사용하는 상위 패키지의 보조 의존성
traitlets==5.14.3  # Jupyter 노트북 실행·통신·문서 처리 지원
truststore==0.10.4  # truststore을(를) 사용하는 상위 패키지의 보조 의존성
transformers==4.52.2  # Transformer 모델 로드·학습·추론
typer==0.15.4  # typer을(를) 사용하는 상위 패키지의 보조 의존성
types-python-dateutil==2.9.0.20250516  # python-dateutil용 정적 타입 정보
types-PyYAML==6.0.12.20260815  # PyYAML용 정적 타입 정보
types-requests==2.32.0.20250515  # requests용 정적 타입 정보
typing-inspect==0.9.0  # 타입 검사·데이터 모델·직렬화 지원
typing-inspection==0.4.0  # 타입 검사·데이터 모델·직렬화 지원
typing-extensions>=4.14,<5  # 타입 검사·데이터 모델·직렬화 지원
tzdata==2026.3  # tzdata을(를) 사용하는 상위 패키지의 보조 의존성
ujson==5.10.0  # 구조화 데이터 검증·직렬화·설정 처리
unstructured==0.17.0  # PDF·Office·HTML 문서 구조화
unstructured-client==0.46.2  # Unstructured 문서 파싱·OCR 기능 지원
unstructured-inference==1.0.2  # Unstructured 문서 파싱·OCR 기능 지원
unstructured.pytesseract==0.3.15  # Unstructured 문서 파싱·OCR 기능 지원
uri-template==1.3.0  # uri-template을(를) 사용하는 상위 패키지의 보조 의존성
urllib3==2.4.0  # HTTP·웹 통신과 인증서·인코딩 처리 지원
uvicorn==0.34.2  # FastAPI 등 ASGI 앱 실행 서버
watchdog==6.0.0  # 파일·패키지·빌드 환경 관리 지원
watchfiles==1.0.5  # 파일·패키지·빌드 환경 관리 지원
wcwidth==0.2.13  # wcwidth을(를) 사용하는 상위 패키지의 보조 의존성
webcolors==24.11.1  # webcolors을(를) 사용하는 상위 패키지의 보조 의존성
webencodings==0.6.1  # webencodings을(를) 사용하는 상위 패키지의 보조 의존성
websocket-client==1.8.0  # HTTP·웹 통신과 인증서·인코딩 처리 지원
websockets==15.0.1  # HTTP·웹 통신과 인증서·인코딩 처리 지원
widgetsnbextension==4.0.16  # widgetsnbextension을(를) 사용하는 상위 패키지의 보조 의존성
wikipedia==1.4.0  # Wikipedia 검색·문서 조회
wrapt==2.3.0  # wrapt을(를) 사용하는 상위 패키지의 보조 의존성
XlsxWriter==3.2.3  # 서식·차트가 포함된 XLSX 생성
xxhash==3.5.0  # xxhash을(를) 사용하는 상위 패키지의 보조 의존성
yarl==1.24.5  # 비동기 네트워크·이벤트 처리 지원
zipp==3.21.0  # 파일·패키지·빌드 환경 관리 지원
zstandard==0.23.0  # zstandard을(를) 사용하는 상위 패키지의 보조 의존성
```
