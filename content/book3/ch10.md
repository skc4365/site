# A2A로 에이전트 연결하기

## 먼저 보는 구조

```text
Agent A
  → Agent Card 확인
  → 작업 요청
  → 상태 확인
  ← 결과 수신
Agent B
```

> MCP가 Agent와 도구의 연결이라면, A2A는 독립된 Agent와 Agent의 연결입니다.

## Agent Card란?

Agent가 자신을 소개하는 정보입니다.

```json
{
  "name": "Greeting Agent",
  "url": "http://127.0.0.1:8010",
  "skills": [
    {"id": "greet", "name": "greet"}
  ]
}
```

호출자는 Card를 보고 주소와 제공 기능을 확인합니다.

## Try — 발견부터 결과까지

```python
import httpx

base_url = "http://127.0.0.1:8010"

card = httpx.get(
    f"{base_url}/.well-known/agent-card.json"
).json()
print("발견한 Agent:", card["name"])

response = httpx.post(
    f"{base_url}/tasks",
    json={"message": "코딩곰"},
)
print(response.json())
```

서버가 먼저 실행 중이어야 합니다.

- [CH10 서버](../course/book3-source-examples/ch10/server.py)
- [CH10 클라이언트](../course/book3-source-examples/ch10/client.py)
- [실행 안내](../course/book3-source-examples/ch10/README.md)

## MCP와 A2A 비교

| 구분 | MCP | A2A |
|---|---|---|
| 연결 | Agent ↔ 도구 | Agent ↔ Agent |
| 상대 | 기능 제공자 | 독립적인 작업 수행자 |
| 핵심 | Tool 발견과 호출 | 능력 발견과 작업 위임 |

## 기억할 내용

1. Agent Card로 상대 Agent의 능력을 발견한다.
2. 작업에는 요청·상태·결과가 있다.
3. 서버별 주소와 시간 초과를 관리한다.

## 다음으로

- SDK 기반 원본은 **소스 10**에서 확인합니다.
- 별도 응용은 **실습 10**에서 진행합니다.
- Chapter 11에서는 지금까지의 요소를 하나로 연결합니다.

